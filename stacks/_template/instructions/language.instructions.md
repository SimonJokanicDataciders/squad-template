<!-- TODO: Add your language/framework-specific coding standards here -->
<!-- This file is loaded by GitHub Copilot as coding instructions -->

<!-- Example for C#:
## C# Conventions
- Use `sealed` by default
- Use `is null` / `is not null` for null checks
- Use file-scoped namespaces
- Use pattern matching and early returns
-->

<!-- Example for TypeScript:
## TypeScript Conventions
- Use strict mode
- Prefer `const` over `let`
- Use explicit return types on public functions
- Use discriminated unions over enums
-->
